import IfcUtil
let fun inputLineWithPini () =
        let pini authority val s = freadln (stdin)
        in s
        end

    fun inputLineAtLevel authority level =
        let val s = inputLineWithPini ()
        in pdeclassify (s, authority, level)
        end

    fun print x = fwrite (stdout, x)

    val _ = print "Please input something: "
    val s = inputLineAtLevel authority `{}`
    val _ =  print ("You have provided input: " ^ s ^ "\n")

in s end
