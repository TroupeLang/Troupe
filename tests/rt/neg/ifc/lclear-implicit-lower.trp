(* negative test; closing a region in a context more sensitive than its floor *)

let val (_, c) = enableRangedReceive (`{}`, `{alice,bob}`, authority)
in  if (true raisedTo`{alice}`)
        then disableRangedReceive c
        else ()
end
