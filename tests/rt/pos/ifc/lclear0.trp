(* positive test; should be ok. The successful close is itself the certification
   check: an uncertified region (ok = false) has no close. *)
let val (_, c) = enableRangedReceive (`<;#root-integrity>`, `<alice;#root-integrity>`, authority)
    val () = disableRangedReceive c
in c
end
