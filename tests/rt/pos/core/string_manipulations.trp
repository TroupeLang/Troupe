import string

let val x = charAt "hello" 0
    val y = "1" ^ "2"
    val s = ("Hello" raisedTo `{alice}`)  ^ ", " ^ ("World" raisedTo `{bob}`)
    val i = 3 raisedTo `{charlie}`
    val j = 5 raisedTo `{dorothy}`
in (x,y,s, substring (s, i, j))
end 