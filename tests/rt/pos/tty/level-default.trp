require native Tty

(* ttyLevel reports the channel level, which defaults to ROOT. *)
let val a = authority
    val i = stdin
    val o = stdout
    val _ = print (ttyLevel i)
    val _ = print (ttyLevel o)
in ()
end
