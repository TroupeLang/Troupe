(* ttySize takes an output descriptor. Handing it the stdin descriptor is a
   programming error and kills the thread, the same disposition fwrite uses for
   an input descriptor. *)
let val a = authority
    val i = stdin a
in print (ttySize i)
end
