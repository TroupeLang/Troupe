(* Terminal events come from the input channel, so `ttySubscribe` accepts only
   the stdin descriptor. Handing it an output descriptor is a programming
   error and kills the thread, as it does for `ttyRawMode`. *)
let val a = authority
    val o = stdout
    val me = self ()
    val _ = ttySubscribe (o, me)
in ()
end
