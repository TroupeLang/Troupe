# What this test writes sits above root integrity -- probe values raised to
# {alice}, and prints made while the blocking label is raised -- so the channel
# level is the pre-integrity label model, which is what the test was written
# against.
--stdiolev '<#root-confidentiality;#null-integrity>'
