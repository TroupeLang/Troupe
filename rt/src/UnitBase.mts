import * as levels from './options.mjs'


export let __unitbase = { stringRep: () => "()"
              , _is_unit : true
              , _troupeType : 0 /* UNIT */
              , dataLevel : levels.BOT 
              } ;
