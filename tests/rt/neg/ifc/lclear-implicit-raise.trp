(* negative test; this program attempts to open a ranged-receive region in a
   high context, violating the non-sensitive upgrade like property of the mailbox
   semantics
*)

if (true raisedTo `{alice}`)
        then enableRangedReceive (`{}`, `{alice}`, authority)
        else ()

