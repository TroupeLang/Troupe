(* implicit flows and type labels *)
let val x = 1 raisedTo {alice}
    val y = 2 raisedTo {bob}
    val z = 3 raisedTo {charlie}
    val t = pinipush authority
    val w = x + (if y > 2 then z else -z)
    val _ = pinipop t
in printWithLabels w
end
