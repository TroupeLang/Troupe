(* blocking; there is sufficient clearance to 
   perform the receive *)

let val _ = enableRangedReceive (`{}`, `{alice}`, attenuate (authority, `<#null-confidentiality;#null-integrity>`))
in rcv (`{}`, `{alice}`, [] )
end


