let val main = self () 
    fun f1 () = send (main, 1); send (main, 2)
    val _ = spawn (f1)
    val _ = enableRangedReceive (`<;#root-integrity>`, `<alice;#root-integrity>`, attenuate (authority, `<#null-confidentiality;#null-integrity>`))
    val p = pinipush (authority)
    val _ = debugpc()
    val u = rcv (`{}`, `<alice;#root-integrity>`, [hn _ => 42]) 
    val _ = pinipop (p)
    val _ = debugpc ()
    val v = rcv (`{}`, `{}`, [hn x => x ])
in adv v
end