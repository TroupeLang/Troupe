(* negative test; lowering clearance in a context than the original *)

let val c = raisembox (`{alice,bob}`)        
in  if (true raisedTo`{alice}`) 
        then lowermbox (c, authority)
        else ()     
end
