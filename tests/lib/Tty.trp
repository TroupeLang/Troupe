(* The Tty cases at the default channel level. The cases themselves live in
 * modsrc/TtyCases so that one copy serves every level a driver runs them at;
 * Tty-alice.trp is the same cases at {alice}. *)
import "./modsrc/TtyCases"

TtyCases.run authority
